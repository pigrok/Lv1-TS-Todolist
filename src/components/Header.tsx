import styled from "styled-components";

const Header = () => {
  return <Head>BUGGER</Head>;
};

export default Header;

const Head = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;

  padding: 60px;

  background-color: #df9711cd;

  font-size: 40px;

  border-radius: 80px 80px 0 0;

  margin-top: 40px;
`;
